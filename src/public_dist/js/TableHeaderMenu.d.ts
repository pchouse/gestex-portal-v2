import { MenuObject, ColumnComponent } from 'tabulator-tables';
/**
 * Create the header menu to hide/show columns
 * @constructor
 */
export declare var TableHeaderMenu: MenuObject<ColumnComponent>[];
